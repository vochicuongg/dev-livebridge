# AGENTS.md

## Git workflow requirement

After every code change, always do:

1. `git add -A`
2. `git commit -m "<short clear message>"`
3. `git push`

Do not leave local unpushed changes after completing a task.
